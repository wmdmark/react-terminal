// Generated by CoffeeScript 1.9.3
var Line, Prompt, Terminal;

Line = require("./Line");

Prompt = require("./Prompt");

require("./style/Terminal.css");

Terminal = function(props) {
  var cursorPosition, history, input, prompt;
  input = props.input, cursorPosition = props.cursorPosition, history = props.history, prompt = props.prompt;
  return React.createElement("div", {
    "className": "Terminal"
  }, history.map(function(line, index) {
    return React.createElement(Line, {
      "key": index,
      "line": line
    });
  }), (prompt != null ? React.createElement(Prompt, {
    "prompt": prompt,
    "input": input,
    "cursorPosition": cursorPosition
  }) : void 0));
};

Terminal.displayName = "Terminal";

module.exports = Terminal;
