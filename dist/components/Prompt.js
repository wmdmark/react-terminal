// Generated by CoffeeScript 1.9.3
var Prompt;

Prompt = function(props) {
  var cursorChar, cursorPosition, input, linput, prompt, rinput;
  input = props.input, prompt = props.prompt, cursorPosition = props.cursorPosition;
  if (cursorPosition > 0) {
    linput = input.substring(0, cursorPosition);
  }
  if (cursorPosition < input.length) {
    cursorChar = input[cursorPosition];
  } else {
    cursorChar = "\u00a0";
  }
  rinput = input.substring(cursorPosition + 1, input.length);
  return React.createElement("div", {
    "className": "Prompt"
  }, React.createElement("span", {
    "className": "Prompt__from"
  }, prompt), (linput ? React.createElement("span", null, linput) : void 0), React.createElement("span", {
    "className": "Prompt__cursor"
  }, cursorChar), React.createElement("span", {
    "className": "Prompt__input"
  }, rinput));
};

Prompt.displayName = "Prompt";

module.exports = Prompt;
